{
  "name": "radio-ninada-platform",
  "version": "1.0.0",
  "description": "Radio Ninada 90.4 FM Full-Stack Streaming Platform & Studio CMS",
  "scripts": {
    "postinstall": "cd backend && npm install && npm run prisma:generate",
    "build": "cd backend && npm run prisma:generate && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "test": "cd backend && npm test",
    "db:push": "cd backend && npm run db:push",
    "db:seed": "cd backend && npm run db:seed"
  },
  "dependencies": {
    "@neon/config": "^1.3.0",
    "@neon/env": "^1.2.1"
  }
}
